"use strict";
let names = ["Karar", "Saad", "Arshad", "Sammi", "Rahees", "Yunus"];
let message = "Shall we do coding night again?";
// for (let i = 0; i < names.length; i++) {
//   console.log(`${names[i]}, ${message}`);
// }
names.forEach(name => {
    console.log(`${name}, ${message}`);
});
